# Fluentd Logging Test (PHP)

## Running
It will print out the time and fluentd is configured to discard. If you wish to see the messages change null to `stdout`
```
docker-compose up --abort-on-container-exit   
```
